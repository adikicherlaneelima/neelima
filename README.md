# Stock Price Prediction (Simple)

## Description
This is a simple stock prediction app using Streamlit and Moving Average.

## How to Run
1. Install dependencies:
   pip install -r requirements.txt

2. Run app:
   streamlit run app.py

## Input
Upload CSV file with:
Date, Close

## Output
- Displays stock data
- Shows moving average
- Predicts next price